repos:
  - repo: local
    hooks:
      - id: commitlint
        name: Validate commit message format
        entry: .githooks/commitlint-wrapper.py
        language: python
        stages: [commit-msg] 